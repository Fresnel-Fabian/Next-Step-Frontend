/**
 * API Configuration
 *
 * For physical device (Expo Go on phone): 127.0.0.1 won't work - use your computer's IP.
 * Run: ipconfig getifaddr en0  (Mac) to get it, then set below.
 *
 * For simulator/emulator: leave as null to use 127.0.0.1 (iOS) or 10.0.2.2 (Android).
 */

// Physical device: set to your computer's IP. Run: ipconfig getifaddr en0
// e.g. 'http://192.168.1.5:8000/'  — replace with YOUR IP
// Simulator/emulator: leave null
export const API_URL: string | null = null;
